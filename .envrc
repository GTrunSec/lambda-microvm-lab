watch_file flake.nix flake.lock \
    shell/* \
    shell/microvm-hunting-lab/*
{
    # shell gc root dir
    mkdir -p "$(direnv_layout_dir)"

    eval "$(nix print-dev-env --profile $(direnv_layout_dir)/flake-profile)"
} || use nix
